<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="176dp"
    android:padding="16dp"
    android:theme="@style/ThemeOverlay.AppCompat.Dark"
    android:background="@color/skyBlue"
    android:gravity="bottom">

    <TextView
        android:id="@+id/favLocationsHeading"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingBottom="4dp"
        android:textSize="20sp"
        android:text="@string/fav_locs_heading"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <ImageView
        android:id="@+id/addLocation"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="15dp"
        android:src="@drawable/plus"
        app:layout_constraintBottom_toBottomOf="@+id/favLocationsHeading"
        app:layout_constraintStart_toEndOf="@+id/favLocationsHeading"
        app:layout_constraintTop_toTopOf="@+id/favLocationsHeading" />

    <ImageView
        android:id="@+id/deleteLocation"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="15dp"
        android:src="@drawable/minus"
        app:layout_constraintBottom_toBottomOf="@+id/favLocationsHeading"
        app:layout_constraintStart_toEndOf="@+id/addLocation"
        app:layout_constraintTop_toTopOf="@+id/favLocationsHeading"/>

</androidx.constraintlayout.widget.ConstraintLayout>